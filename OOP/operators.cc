#include "iostream"
using namespace std;

class Point {
    private:
        ostringstream data;
        double x, y;
    public:

        Point(double x, double y) x(x), y(y){}

        double getX() const {
            return x;
        }
        
        double gety() const {
            return y;
        }

        Point operator+(){
            return 
        }

};


int main(int argc, char const *argv[])
{
    
    return 0;
}
